import { StyleSheet, Text, View } from "react-native";
import React from "react";

const VerticalCard = () => {
  return (
    <View>
      <Text>VerticalCard</Text>
    </View>
  );
};

export default VerticalCard;

const styles = StyleSheet.create({});
